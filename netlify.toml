# Netlify configuration for Velo Altitude
[build]
  command = "npm run build --prefix client"
  publish = "client/build"

[build.environment]
  NODE_VERSION = "18.18.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
