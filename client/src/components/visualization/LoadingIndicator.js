import React from 'react';
import { CircularProgress } from '@mui/material';

/**
 * LoadingIndicator
 * - Indicateur circulaire de chargement centré
 */
const LoadingIndicator = () => <CircularProgress size={48} thickness={4} color="primary" />;

export default LoadingIndicator;
